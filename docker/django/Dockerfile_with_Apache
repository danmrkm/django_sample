FROM python:3.8.0
ENV PYTHONUNBUFFERED 1

RUN mkdir /code && apt-get update && apt-get -y install apache2 curl

WORKDIR /code
ADD requirements.txt /code/
RUN pip install -r requirements.txt
ADD ./sample_project /code/
